/** Versioned localStorage keys. Bump the version suffix when the stored schema changes. */
export const STORAGE_KEYS = {
    apiKey: 'tmdb_api_key:v1',
    watchlist: 'watchmood_list:v1',
} as const;
